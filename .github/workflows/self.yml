---
name: Self
on: [push, pull_request]

jobs:
  latest:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: latest
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: ${{ github.event_name == 'pull_request' && 'pnpm' || '' }}

      # ensure that a PR that doesn't update the dist actually uses the
      # updated code when it's running the self step
      - if: github.event_name == 'pull_request'
        run: pnpm install && pnpm run build && pnpm run package

      - uses: ./
      - if: ${{ matrix.os == 'windows-latest' }}
        run: shfmt.exe -d test.bash
      - if: ${{ matrix.os != 'windows-latest' }}
        run: shfmt -d test.bash

  fixed:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: latest
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: ${{ github.event_name == 'pull_request' && 'pnpm' || '' }}

      # ensure that a PR that doesn't update the dist actually uses the
      # updated code when it's running the self step
      - if: github.event_name == 'pull_request'
        run: pnpm install && pnpm run build && pnpm run package

      - uses: ./
        with:
          shfmt-version: 3.3.1
      - if: ${{ matrix.os == 'windows-latest' }}
        run: shfmt.exe -d test.bash
      - if: ${{ matrix.os != 'windows-latest' }}
        run: shfmt -d test.bash
